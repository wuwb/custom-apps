version: '3'

services:
  alist:
    image: xhofe/alist:latest
    container_name: alist
    restart: always
    ports:
      - '5244:5244'
    volumes:
      - ../../../custom-apps-data/alist:/opt/alist/data

